# Domain Docs

This is a single-context repository.

Before changing domain behavior:

- read `CONTEXT.md`;
- read every accepted RFC governing the behavior;
- read relevant ADRs under `docs/adr/`.

Use the glossary's canonical vocabulary in code, tests, specs, and tickets. Surface conflicts with an accepted ADR or RFC instead of silently overriding them.
